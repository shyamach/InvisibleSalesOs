node_modules/

# Environment & secrets — NEVER commit these
.env
.env.*
.env.local
.env.*.local
google-credentials.json
*.pem
*.key
service-account*.json

# WhatsApp Web browser session cache
.wwebjs_auth/
.wwebjs_cache/

# OS
.DS_Store

# Build outputs
dist/
build/
.next/
frontend/.next/
frontend/out/
coverage/

# Vite/Vitest cache artefacts (generated at runtime, never commit)
vitest.config.js.timestamp-*.mjs

# Supabase CLI cache
supabase/.temp/
