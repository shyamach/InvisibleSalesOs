# This project uses system Chrome (configured in server.js via executablePath).
# Puppeteer's bundled browser download is disabled to prevent install failures.
# The PUPPETEER_SKIP_DOWNLOAD env var must be set in the shell OR via `npm config set` globally.
# See: https://pptr.dev/guides/configuration
